# Path to rembg executable (optional, defaults to ~/.local/bin/rembg)
REMBG_PATH=~/.local/bin/rembg

# Directory to scan for images (optional, defaults to ~/Downloads)
IMAGES_DIR=~/Downloads

# Base directory for output (optional, defaults to ~/Downloads)
# The subfolder name is configured in Raycast preferences
OUTPUT_DIR=~/Downloads
